#ifndef FILENAMAGER_HPP
#define FILENAMAGER_HPP

using namespace std;
class FileManager {
private:
    // SyncIn: Detecta modificações locais (loop)
    // Pull: Recebe modificações do servidor (chamado pelo communication manager)
public:

};

#endif // FILEMANAGE_HPP
